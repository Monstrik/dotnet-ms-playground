apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-service
  namespace: foodorder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weather-service
  template:
    metadata:
      labels:
        app: weather-service
    spec:
      containers:
        - name: weather-service
          image: weather-service:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_URLS
              value: http://+:8080

---
apiVersion: v1
kind: Service
metadata:
  name: weather-service
  namespace: foodorder
spec:
  selector:
    app: weather-service
  ports:
    - port: 8080
      targetPort: 8080

